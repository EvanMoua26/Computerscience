def add ():
    print("Add two number:")
    x = input("what is the first number?\n>")
    x = int(x)
    #convert x to int
    y = input("What is the second number?\n>")
    y = int(y)
    #convert y to int
    print(str(x) + " + " + str(y) + " = " + str( x +  y ))

def Subtract ():
    print("subtract two number:")
    x = input("what is the first number?\n>")
    x(int)(x)
    #convert x to int
    y = input("what is the second number??\n>")
    y = (int)(y)
    print(str(x) + " + " + str(y) + " = " + str( x +  y ))
def mulitply ():
    print("mulitply two number:")
    x = input("what is the first number?\n>")
    x(int)(x)
    #convert x to int
    y = input("what is the second number??\n>")
    y = (int)(y)
    print(str(x) + " * " + str(y) + " = " + str( x * y ))
def divide ():
    print("divide two number:")
    x = input("what is the first number?\n>")
    x(int)(x)
    #convert x to int
    y = input("what is the second number??\n>")
    y = (int)(y)
    print(str(x) + " // " + str(y) + " = " + str( x // y ))

